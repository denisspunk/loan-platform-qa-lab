<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>lab</groupId>
    <artifactId>loan-platform-qa-lab</artifactId>
    <version>1.0-SNAPSHOT</version>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <jackson.version>2.22.2</jackson.version>
        <junit.version>5.14.4</junit.version>
        <assertj.version>3.27.7</assertj.version>
        <mockito.version>5.23.0</mockito.version>
        <rest-assured.version>5.5.7</rest-assured.version>
        <wiremock.version>3.13.2</wiremock.version>
        <awaitility.version>4.3.0</awaitility.version>
        <slf4j.version>2.0.19</slf4j.version>
        <postgresql.version>42.7.13</postgresql.version>
        <testcontainers.version>2.0.5</testcontainers.version>
        <jacoco.version>0.8.13</jacoco.version>

        <!-- jacoco:prepare-agent fills this in; empty by default so surefire still starts
             when coverage is off (-Djacoco.skip=true) -->
        <jacocoArgLine></jacocoArgLine>

        <!-- tests against a deployed stand run only on request:
             mvn test -Dgroups="smoke | regression" -DexcludedGroups= -Dlab.baseUrl=https://... -->
        <excludedGroups>smoke | regression</excludedGroups>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- the service itself: JSON only, the HTTP server comes with the JDK -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>
        <!-- the service: Postgres driver, used when DATABASE_URL is set -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>${postgresql.version}</version>
        </dependency>

        <!-- core: runner and assertions -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.awaitility</groupId>
            <artifactId>awaitility</artifactId>
            <version>${awaitility.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- unit: test doubles -->
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- clients: HTTP and JSON schema -->
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <version>${rest-assured.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>json-schema-validator</artifactId>
            <version>${rest-assured.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- stubs: the device-lock partner -->
        <dependency>
            <groupId>org.wiremock</groupId>
            <artifactId>wiremock-standalone</artifactId>
            <version>${wiremock.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <version>${slf4j.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- integration: a real Postgres in Docker for the JDBC repository -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-postgresql</artifactId>
            <version>${testcontainers.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-junit-jupiter</artifactId>
            <version>${testcontainers.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.15.0</version>
            </plugin>
            <plugin>
                <!-- exposes ${org.mockito:mockito-core:jar} so Mockito loads as an agent, without JDK warnings -->
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.8.1</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>properties</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <!-- runs JUnit; -Dgroups=unit picks one pyramid level by @Tag -->
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.6</version>
                <configuration>
                    <!-- @{...} is resolved when the fork starts, not when the pom is read, so the
                         JaCoCo agent set later by prepare-agent lands here next to Mockito's -->
                    <argLine>@{jacocoArgLine} -javaagent:${org.mockito:mockito-core:jar} -Xshare:off</argLine>
                </configuration>
            </plugin>
            <plugin>
                <!-- code coverage; declared after surefire so report runs once the tests are done -->
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <!-- puts -javaagent:...jacocoagent.jar into ${jacocoArgLine} -->
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                        <configuration>
                            <propertyName>jacocoArgLine</propertyName>
                            <!-- JaCoCo appends by default, so without a clean each local run would
                                 quietly add to the one before and report more than it covered.
                                 Safe here because surefire uses a single fork; with forkCount > 1
                                 the forks would overwrite each other and this has to go back to true -->
                            <append>false</append>
                            <!-- instrument the service and nothing else: the tests are not the
                                 subject, and the JDK's own HTTP server classes only add log noise -->
                            <includes>
                                <include>lab.loans.*</include>
                            </includes>
                            <!-- Mockito builds its mocks for the JVM it runs on, so on a JDK newer
                                 than JaCoCo supports they fail to instrument; they are not our code -->
                            <excludes>
                                <exclude>*MockitoMock*</exclude>
                                <exclude>*$auxiliary*</exclude>
                            </excludes>
                        </configuration>
                    </execution>
                    <execution>
                        <!-- one level, one JVM: target/site/jacoco/index.html -->
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <!-- CI only: mvn jacoco:merge@merge-exec over the .exec files the
                             pyramid jobs collected into target/coverage-exec/ -->
                        <id>merge-exec</id>
                        <phase>none</phase>
                        <goals>
                            <goal>merge</goal>
                        </goals>
                        <configuration>
                            <fileSets>
                                <fileSet>
                                    <directory>${project.build.directory}/coverage-exec</directory>
                                    <includes>
                                        <include>*.exec</include>
                                    </includes>
                                </fileSet>
                            </fileSets>
                            <destFile>${project.build.directory}/jacoco-merged.exec</destFile>
                        </configuration>
                    </execution>
                    <execution>
                        <!-- CI only: mvn jacoco:report@merged-report, the whole pyramid at once -->
                        <id>merged-report</id>
                        <phase>none</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                        <configuration>
                            <dataFile>${project.build.directory}/jacoco-merged.exec</dataFile>
                            <outputDirectory>${project.build.directory}/site/jacoco-merged</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
